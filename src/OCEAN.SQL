CREATE TABLE COUNTRY (
    ID            SMALLINT NOT NULL,
    NODC_CODE     VARCHAR(4) CHARACTER SET UTF8,
    ISO3166_CODE  VARCHAR(2) CHARACTER SET UTF8 NOT NULL,
    NAME          VARCHAR(50) CHARACTER SET UTF8 NOT NULL,
    NOTES         BLOB SUB_TYPE 1 SEGMENT SIZE 8192 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE CRUISE (
    ID                      BIGINT NOT NULL,
    PLATFORM_ID             BIGINT DEFAULT 489 NOT NULL,
    SOURCE_ID               SMALLINT DEFAULT 1 NOT NULL,
    INSTITUTE_ID            SMALLINT DEFAULT 1 NOT NULL,
    PROJECT_ID              SMALLINT DEFAULT 0 NOT NULL,
    EXPOCODE                VARCHAR(50) CHARACTER SET UTF8,
    WOD_CRUISE_CODE         VARCHAR(10) CHARACTER SET UTF8,
    CRUISE_NUMBER           VARCHAR(100) CHARACTER SET UTF8,
    DATE_START_TOTAL        TIMESTAMP,
    DATE_END_TOTAL          TIMESTAMP,
    DATE_START_DATABASE     TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    DATE_END_DATABASE       TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    PRINCIPAL_INVESTIGATOR  VARCHAR(255) CHARACTER SET UTF8,
    NOTES                   BLOB SUB_TYPE TEXT SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED              TIMESTAMP NOT NULL,
    DATE_UPDATED            TIMESTAMP NOT NULL,
    LATITUDE_MIN            DECIMAL(8,5) DEFAULT 0 NOT NULL,
    LATITUDE_MAX            DECIMAL(8,5) DEFAULT 0 NOT NULL,
    LONGITUDE_MIN           DECIMAL(9,5) DEFAULT 0 NOT NULL,
    LONGITUDE_MAX           DECIMAL(9,5) DEFAULT 0 NOT NULL,
    SELECTED                BOOLEAN DEFAULT false NOT NULL,
    DUPLICATE               BOOLEAN DEFAULT false NOT NULL,
    STATIONS_TOTAL          INTEGER,
    STATIONS_DATABASE       INTEGER DEFAULT 0 NOT NULL,
    STATIONS_DUPLICATES     INTEGER DEFAULT 0 NOT NULL,
    DOI                     VARCHAR(255) CHARACTER SET UTF8,
    WWW                     VARCHAR(255) CHARACTER SET UTF8
);

CREATE TABLE DATABASE_INFO (
    ID                     SMALLINT NOT NULL,
    STATION_COUNT          BIGINT NOT NULL,
    STATION_ID_MIN         BIGINT NOT NULL,
    STATION_ID_MAX         BIGINT NOT NULL,
    LATITUDE_MIN           DOUBLE PRECISION NOT NULL,
    LATITUDE_MAX           DOUBLE PRECISION NOT NULL,
    LONGITUDE_MIN          DOUBLE PRECISION NOT NULL,
    LONGITUDE_MAX          DOUBLE PRECISION NOT NULL,
    DATEANDTIME_MIN        TIMESTAMP NOT NULL,
    DATEANDTIME_MAX        TIMESTAMP NOT NULL,
    DATE_ADDED_MIN         TIMESTAMP NOT NULL,
    DATE_ADDED_MAX         TIMESTAMP NOT NULL,
    DATE_UPDATED_MIN       TIMESTAMP NOT NULL,
    DATE_UPDATED_MAX       TIMESTAMP NOT NULL,
    CRUISE_COUNT           INTEGER NOT NULL,
    CRUISE_ID_MIN          INTEGER NOT NULL,
    CRUISE_ID_MAX          INTEGER NOT NULL,
    CRUISE_ST_TOTAL_MAX    INTEGER NOT NULL,
    CRUISE_ST_DB_MAX       INTEGER NOT NULL,
    CRUISE_DUPLICATES_MAX  INTEGER NOT NULL
);

CREATE TABLE DATABASE_TABLES (
    ID                BIGINT NOT NULL,
    NAME_TABLE        VARCHAR(31) CHARACTER SET UTF8 NOT NULL,
    NAME_ARGO         VARCHAR(20) CHARACTER SET UTF8,
    NAME              VARCHAR(255) CHARACTER SET UTF8,
    EXTERNAL_TBL      BOOLEAN DEFAULT false NOT NULL,
    UNITS_ID_DEFAULT  SMALLINT NOT NULL,
    UNITS_RANGE_MIN   DOUBLE PRECISION,
    UNITS_RANGE_MAX   DOUBLE PRECISION,
    NOTES             BLOB SUB_TYPE TEXT SEGMENT SIZE 8192 CHARACTER SET UTF8,
    DATE_ADDED        TIMESTAMP NOT NULL,
    DATE_UPDATED      TIMESTAMP NOT NULL
);

CREATE TABLE ENTRY (
    ID               BIGINT NOT NULL,
    ENTRY_TYPE_ID    BIGINT NOT NULL,
    TITLE            VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    DATE_START       TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    DATE_END         TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    STATIONS_AMOUNT  BIGINT DEFAULT 0 NOT NULL,
    DATE_ADDED       TIMESTAMP NOT NULL,
    DATE_UPDATED     TIMESTAMP NOT NULL,
    SELECTED         BOOLEAN DEFAULT false NOT NULL,
    NOTES            BLOB SUB_TYPE 1 SEGMENT SIZE 8192 CHARACTER SET UTF8
);

CREATE TABLE ENTRY_TYPE (
    ID           BIGINT NOT NULL,
    NAME         VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    DESCRIPTION  BLOB SUB_TYPE 1 SEGMENT SIZE 16384
);

CREATE TABLE FLAG_PQF1 (
    ID            SMALLINT NOT NULL,
    NAME          VARCHAR(256) CHARACTER SET UTF8 NOT NULL,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE FLAG_PQF2 (
    ID            SMALLINT NOT NULL,
    NAME          VARCHAR(256) CHARACTER SET UTF8 NOT NULL,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE FLAG_SQF (
    ID            SMALLINT NOT NULL,
    NAME          VARCHAR(256) CHARACTER SET UTF8 NOT NULL,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE FLAG_STATION (
    ID            SMALLINT NOT NULL,
    NAME          VARCHAR(256) CHARACTER SET UTF8 NOT NULL,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE INSTITUTE (
    ID            SMALLINT DEFAULT 1 NOT NULL,
    NODC_CODE     VARCHAR(4) CHARACTER SET UTF8,
    WOD_ID        BIGINT,
    NAME          VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    NOTES         BLOB SUB_TYPE TEXT SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL,
    PLACE         VARCHAR(50) CHARACTER SET UTF8,
    NAME_NATIVE   VARCHAR(255) CHARACTER SET UTF8,
    COUNTRY_ID    SMALLINT DEFAULT 1 NOT NULL
);

CREATE TABLE INSTRUMENT (
    ID            SMALLINT NOT NULL,
    WOD_ID        SMALLINT,
    NAME          VARCHAR(15) CHARACTER SET UTF8 NOT NULL,
    NOTES         BLOB SUB_TYPE TEXT SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE METEO (
    ID           BIGINT NOT NULL,
    TEMPDRY      DECIMAL(5,2),
    TEMPWET      DECIMAL(5,2),
    PRESSURE     DECIMAL(5,1),
    WINDDIR      SMALLINT,
    WINDSPEED    DECIMAL(5,1),
    CLOUDCOMMON  SMALLINT,
    CLOUDLOW     SMALLINT,
    CLOUDTYPE    VARCHAR(20) CHARACTER SET UTF8,
    VISIBILITY   SMALLINT,
    HUMABS       DECIMAL(4,1),
    HUMREL       SMALLINT,
    WAVEHEIGHT   DECIMAL(5,1),
    WAVEDIR      SMALLINT,
    WAVEPERIOD   SMALLINT,
    SEASTATE     SMALLINT,
    WEATHER      SMALLINT,
    WATERCOLOR   SMALLINT,
    WATERTRANSP  SMALLINT,
    SURFTEMP     DECIMAL(5,2),
    SURFSALT     DECIMAL(5,2)
);

CREATE TABLE PLATFORM (
    ID            BIGINT NOT NULL,
    NODC_CODE     VARCHAR(4) CHARACTER SET UTF8,
    WOD_ID        BIGINT DEFAULT -9,
    IMO_ID        INTEGER DEFAULT -9,
    CALLSIGN      VARCHAR(20) CHARACTER SET UTF8,
    COUNTRY_ID    SMALLINT NOT NULL,
    TITLE         VARCHAR(10) CHARACTER SET UTF8,
    NAME          VARCHAR(50) CHARACTER SET UTF8 NOT NULL,
    NAME_ICES     VARCHAR(50) CHARACTER SET UTF8,
    NAME_WOD      VARCHAR(50) CHARACTER SET UTF8,
    NAME_NATIVE   VARCHAR(50) CHARACTER SET UTF8,
    NOTES         BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    NOTES_ICES    BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    NOTES_WOD     BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE PROJECT (
    ID            SMALLINT NOT NULL,
    WOD_ID        BIGINT DEFAULT -9,
    NAME          VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    NOTES         BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);

CREATE TABLE SOURCE (
    ID              SMALLINT NOT NULL,
    NAME            VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    PRIORITY        SMALLINT,
    STATION_ID_MIN  BIGINT,
    STATION_ID_MAX  BIGINT,
    NOTES           BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED      TIMESTAMP NOT NULL,
    DATE_UPDATED    TIMESTAMP NOT NULL
);

CREATE TABLE STATION (
    ID                 BIGINT NOT NULL,
    LATITUDE           DECIMAL(8,5) NOT NULL,
    LONGITUDE          DECIMAL(9,5) NOT NULL,
    DATEANDTIME        TIMESTAMP NOT NULL,
    BOTTOMDEPTH        INTEGER,
    BOTTOMDEPTH_GEBCO  INTEGER,
    LASTLEVEL_M        INTEGER,
    LASTLEVEL_DBAR     INTEGER,
    CRUISE_ID          BIGINT DEFAULT 0 NOT NULL,
    ST_NUMBER_ORIGIN   VARCHAR(50) CHARACTER SET UTF8,
    ST_ID_ORIGIN       BIGINT,
    QCFLAG             SMALLINT DEFAULT 0 NOT NULL,
    STVERSION          SMALLINT DEFAULT 0 NOT NULL,
    CAST_NUMBER        INTEGER DEFAULT 1 NOT NULL,
    DUPLICATE          BOOLEAN DEFAULT false NOT NULL,
    EMPTY              BOOLEAN DEFAULT false,
    SELECTED           BOOLEAN DEFAULT false NOT NULL,
    DATE_ADDED         TIMESTAMP NOT NULL,
    DATE_UPDATED       TIMESTAMP NOT NULL,
    FILE_PATH          VARCHAR(50) CHARACTER SET UTF8
);

CREATE TABLE STATION_ENTRY (
    STATION_ID  BIGINT NOT NULL,
    ENTRY_ID    BIGINT NOT NULL
);

CREATE TABLE STATION_PARAMETERS (
    STATION_ID  BIGINT NOT NULL,
    TABLE_ID    BIGINT NOT NULL
);

CREATE TABLE TEMPORARY_ID_LIST (
    ID  BIGINT NOT NULL
);

CREATE TABLE UNITS (
    ID            SMALLINT NOT NULL,
    NAME_SHORT    VARCHAR(20) CHARACTER SET UTF8 NOT NULL,
    NAME          VARCHAR(255) CHARACTER SET UTF8 NOT NULL,
    "LENGTH"      SMALLINT,
    SCALE         SMALLINT,
    NOTES         BLOB SUB_TYPE 1 SEGMENT SIZE 16384 CHARACTER SET UTF8,
    DATE_ADDED    TIMESTAMP NOT NULL,
    DATE_UPDATED  TIMESTAMP NOT NULL
);



/******************************************************************************/
/***                           Unique constraints                           ***/
/******************************************************************************/

ALTER TABLE DATABASE_TABLES ADD CONSTRAINT UNQ1_DATABASE_TABLES UNIQUE (NAME_TABLE);
ALTER TABLE STATION ADD CONSTRAINT UNQ1_STATION UNIQUE (LATITUDE, LONGITUDE, DATEANDTIME, STVERSION, CAST_NUMBER);
ALTER TABLE STATION_PARAMETERS ADD CONSTRAINT UNQ1_STATION_PARAMETERS UNIQUE (STATION_ID, TABLE_ID);
ALTER TABLE UNITS ADD CONSTRAINT UNQ1_UNITS UNIQUE (NAME_SHORT, NAME);


/******************************************************************************/
/***                              Primary keys                              ***/
/******************************************************************************/

ALTER TABLE COUNTRY ADD CONSTRAINT PK_COUNTRY PRIMARY KEY (ID);
ALTER TABLE CRUISE ADD CONSTRAINT PK_CRUISE PRIMARY KEY (ID);
ALTER TABLE DATABASE_INFO ADD CONSTRAINT PK_DATABASE_INFO PRIMARY KEY (ID);
ALTER TABLE DATABASE_TABLES ADD CONSTRAINT DATABASE_TABLES_PK PRIMARY KEY (ID);
ALTER TABLE ENTRY ADD CONSTRAINT ENTRY_PK PRIMARY KEY (ID);
ALTER TABLE ENTRY_TYPE ADD CONSTRAINT ENTRY_TYPE_PK PRIMARY KEY (ID);
ALTER TABLE FLAG_PQF1 ADD CONSTRAINT PK_FLAG_PQF1_1 PRIMARY KEY (ID);
ALTER TABLE FLAG_PQF2 ADD CONSTRAINT PK_FLAG_PQF2_1 PRIMARY KEY (ID);
ALTER TABLE FLAG_SQF ADD CONSTRAINT PK_FLAG_SQF_1 PRIMARY KEY (ID);
ALTER TABLE FLAG_STATION ADD CONSTRAINT PK_FLAG_STATION PRIMARY KEY (ID);
ALTER TABLE INSTITUTE ADD CONSTRAINT PK_INSTITUTE PRIMARY KEY (ID);
ALTER TABLE INSTRUMENT ADD CONSTRAINT PK_INSTRUMENT PRIMARY KEY (ID);
ALTER TABLE PLATFORM ADD CONSTRAINT PK_PLATFORM PRIMARY KEY (ID);
ALTER TABLE PROJECT ADD CONSTRAINT PK_PROJECT PRIMARY KEY (ID);
ALTER TABLE SOURCE ADD CONSTRAINT PK_SOURCE PRIMARY KEY (ID);
ALTER TABLE STATION ADD CONSTRAINT STATION_PK PRIMARY KEY (ID);
ALTER TABLE TEMPORARY_ID_LIST ADD CONSTRAINT PK_TEMPORARY_ID_LIST PRIMARY KEY (ID);
ALTER TABLE UNITS ADD CONSTRAINT PK_UNITS PRIMARY KEY (ID);


/******************************************************************************/
/***                              Foreign keys                              ***/
/******************************************************************************/

ALTER TABLE INSTITUTE ADD CONSTRAINT FK_INSTITUTE_1 FOREIGN KEY (COUNTRY_ID) REFERENCES COUNTRY (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE CRUISE ADD CONSTRAINT FK_CRUISE_1 FOREIGN KEY (PLATFORM_ID) REFERENCES PLATFORM (ID) ON UPDATE CASCADE;
ALTER TABLE CRUISE ADD CONSTRAINT FK_CRUISE_2 FOREIGN KEY (SOURCE_ID) REFERENCES SOURCE (ID) ON UPDATE CASCADE;
ALTER TABLE CRUISE ADD CONSTRAINT FK_CRUISE_3 FOREIGN KEY (INSTITUTE_ID) REFERENCES INSTITUTE (ID) ON UPDATE CASCADE;
ALTER TABLE CRUISE ADD CONSTRAINT FK_CRUISE_4 FOREIGN KEY (PROJECT_ID) REFERENCES PROJECT (ID) ON UPDATE CASCADE;
ALTER TABLE DATABASE_TABLES ADD CONSTRAINT FK_DATABASE_TABLES_1 FOREIGN KEY (UNITS_ID_DEFAULT) REFERENCES UNITS (ID) ON UPDATE CASCADE;
ALTER TABLE ENTRY ADD CONSTRAINT FK_ENTRY_1 FOREIGN KEY (ENTRY_TYPE_ID) REFERENCES ENTRY_TYPE (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE METEO ADD CONSTRAINT FK_METEO FOREIGN KEY (ID) REFERENCES STATION (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE PLATFORM ADD CONSTRAINT FK_PLATFORM_1 FOREIGN KEY (COUNTRY_ID) REFERENCES COUNTRY (ID) ON UPDATE CASCADE;
ALTER TABLE STATION ADD CONSTRAINT FK_STATION_CRUISE FOREIGN KEY (CRUISE_ID) REFERENCES CRUISE (ID) ON DELETE CASCADE ON UPDATE CASCADE
  USING INDEX FK_STATION_1;
ALTER TABLE STATION ADD CONSTRAINT FK_STATION_QCFLAG FOREIGN KEY (QCFLAG) REFERENCES FLAG_STATION (ID) ON DELETE SET DEFAULT ON UPDATE CASCADE
  USING INDEX FK_STATION_2;
ALTER TABLE STATION_ENTRY ADD CONSTRAINT FK_STATION_ENTRY_1 FOREIGN KEY (STATION_ID) REFERENCES STATION (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE STATION_ENTRY ADD CONSTRAINT FK_STATION_ENTRY_2 FOREIGN KEY (ENTRY_ID) REFERENCES ENTRY (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE STATION_PARAMETERS ADD CONSTRAINT FK_STATION_PARAMETERS_1 FOREIGN KEY (STATION_ID) REFERENCES STATION (ID) ON DELETE CASCADE ON UPDATE CASCADE;



/******************************************************************************/
/***                                Indices                                 ***/
/******************************************************************************/


CREATE INDEX STATION_DATEANDTIME_ASC_IDX ON STATION (DATEANDTIME);
CREATE DESCENDING INDEX STATION_DATEANDTIME_DSC_IDX ON STATION (DATEANDTIME);
CREATE INDEX STATION_LATITUDE_ASC_IDX ON STATION (LATITUDE);
CREATE DESCENDING INDEX STATION_LATITUDE_DSC_IDX ON STATION (LATITUDE);
CREATE INDEX STATION_LONGITUDE_ASC_IDX ON STATION (LONGITUDE);
CREATE DESCENDING INDEX STATION_LONGITUDE_DSC_IDX ON STATION (LONGITUDE);


SET TERM ^ ;


/* Trigger: COUNTRY_ADDED */
CREATE TRIGGER COUNTRY_ADDED FOR COUNTRY
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: COUNTRY_UPDATED */
CREATE TRIGGER COUNTRY_UPDATED FOR COUNTRY
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NODC_CODE <> old.NODC_CODE) then new.date_updated = current_timestamp;
 if (new.ISO3166_CODE <> old.ISO3166_CODE) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
end
^

/* Trigger: DATABASE_TABLES_ADDED */
CREATE TRIGGER DATABASE_TABLES_ADDED FOR DATABASE_TABLES
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: DATABASE_TABLES_UPDATED */
CREATE TRIGGER DATABASE_TABLES_UPDATED FOR DATABASE_TABLES
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME_TABLE <> old.NAME_TABLE) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.units_id_default <> old.units_id_default) then new.date_updated = current_timestamp;
end
^

/* Trigger: ENTRY_ADDED */
CREATE TRIGGER ENTRY_ADDED FOR ENTRY
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: ENTRY_UPDATED */
CREATE TRIGGER ENTRY_UPDATED FOR ENTRY
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.ENTRY_TYPE_ID <> old.ENTRY_TYPE_ID) then new.date_updated = current_timestamp;
 if (new.TITLE <> old.TITLE) then new.date_updated = current_timestamp;
 if (new.DATE_START <> old.DATE_START) then new.date_updated = current_timestamp;
 if (new.DATE_END <> old.DATE_END) then new.date_updated = current_timestamp;
 if (new.STATIONS_AMOUNT <> old.STATIONS_AMOUNT) then new.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_PQF1_ADDED */
CREATE TRIGGER FLAG_PQF1_ADDED FOR FLAG_PQF1
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_PQF1_UPDATED */
CREATE TRIGGER FLAG_PQF1_UPDATED FOR FLAG_PQF1
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_PQF2_ADDED */
CREATE TRIGGER FLAG_PQF2_ADDED FOR FLAG_PQF2
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_PQF2_UPDATED */
CREATE TRIGGER FLAG_PQF2_UPDATED FOR FLAG_PQF2
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_SQF_ADDED */
CREATE TRIGGER FLAG_SQF_ADDED FOR FLAG_SQF
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_SQF_UPDATED */
CREATE TRIGGER FLAG_SQF_UPDATED FOR FLAG_SQF
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_STATION_ADDED */
CREATE TRIGGER FLAG_STATION_ADDED FOR FLAG_STATION
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: FLAG_STATION_UPDATED */
CREATE TRIGGER FLAG_STATION_UPDATED FOR FLAG_STATION
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
end
^

/* Trigger: INSTITUTE_ADDED */
CREATE TRIGGER INSTITUTE_ADDED FOR INSTITUTE
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: INSTITUTE_UPDATED */
CREATE TRIGGER INSTITUTE_UPDATED FOR INSTITUTE
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NODC_CODE <> old.NODC_CODE) then new.date_updated = current_timestamp;
 if (new.WOD_ID <> old.WOD_ID) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
end
^

/* Trigger: INSTRUMENT_ADDED */
CREATE TRIGGER INSTRUMENT_ADDED FOR INSTRUMENT
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: INSTRUMENT_UPDATED */
CREATE TRIGGER INSTRUMENT_UPDATED FOR INSTRUMENT
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.WOD_ID <> old.WOD_ID) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
end
^

/* Trigger: PLATFORM_ADDED */
CREATE TRIGGER PLATFORM_ADDED FOR PLATFORM
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: PLATFORM_UPDATED */
CREATE TRIGGER PLATFORM_UPDATED FOR PLATFORM
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NODC_CODE <> old.NODC_CODE) then new.date_updated = current_timestamp;
 if (new.WOD_ID <> old.WOD_ID) then new.date_updated = current_timestamp;
 if (new.IMO_ID <> old.IMO_ID) then new.date_updated = current_timestamp;
 if (new.CALLSIGN <> old.CALLSIGN) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.NAME_NATIVE <> old.NAME_NATIVE) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
 if (new.NOTES_ICES <> old.NOTES_ICES) then new.date_updated = current_timestamp;
 if (new.NOTES_WOD <> old.NOTES_WOD) then new.date_updated = current_timestamp;
 if (new.COUNTRY_ID <> old.COUNTRY_ID) then new.date_updated = current_timestamp;
end
^

/* Trigger: PROJECT_ADDED */
CREATE TRIGGER PROJECT_ADDED FOR PROJECT
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: PROJECT_UPDATED */
CREATE TRIGGER PROJECT_UPDATED FOR PROJECT
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.WOD_ID <> old.WOD_ID) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
end
^

/* Trigger: SOURCE_ADDED */
CREATE TRIGGER SOURCE_ADDED FOR SOURCE
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: SOURCE_UPDATED */
CREATE TRIGGER SOURCE_UPDATED FOR SOURCE
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.STATION_ID_MIN <> old.STATION_ID_MIN) then new.date_updated = current_timestamp;
 if (new.STATION_ID_MAX <> old.STATION_ID_MAX) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
end
^

/* Trigger: UNITS_ADDED */
CREATE TRIGGER UNITS_ADDED FOR UNITS
ACTIVE BEFORE INSERT POSITION 0
AS
begin
 NEW.date_added = current_timestamp;
 NEW.date_updated = current_timestamp;
end
^

/* Trigger: UNITS_UPDATED */
CREATE TRIGGER UNITS_UPDATED FOR UNITS
ACTIVE BEFORE UPDATE POSITION 0
AS
begin
 if (new.NAME_SHORT <> old.NAME_SHORT) then new.date_updated = current_timestamp;
 if (new.NAME <> old.NAME) then new.date_updated = current_timestamp;
 if (new.LENGTH <> old.LENGTH) then new.date_updated = current_timestamp;
 if (new.SCALE <> old.SCALE) then new.date_updated = current_timestamp;
 if (new.NOTES <> old.NOTES) then new.date_updated = current_timestamp;
end
^
SET TERM ; ^

